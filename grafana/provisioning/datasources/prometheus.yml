apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: https://transcendence_prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
      tlsSkipVerify: true
